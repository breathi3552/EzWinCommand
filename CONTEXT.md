# EzWinCommand

EzWinCommand 是可信局域网内连接 Windows 控制端与客户端的单一业务上下文。本词汇表统一 Server、Web 和 Android 对身份、配对、会话及控制状态的称呼。

## 身份与连接

**Server**：
运行在 Windows PC 上、提供控制能力并拥有稳定服务器身份的权威端。Server 不等同于某个网络地址，同一 Server 的地址可以变化。
_Avoid_：服务端实例、PC Agent

**配对请求（Pairing Request）**：
客户端申请成为已配对设备时形成的有限生命周期对象。配对请求不代表设备已经获得访问权限。
_Avoid_：授权码、设备会话

**已配对设备（Paired Device）**：
经 Server 接受、具有独立身份且可被单独撤销的客户端。它表示 Server 认可的设备关系，不等同于客户端当前是否在线。
_Avoid_：客户端、活动会话

**设备会话（Device Session）**：
客户端保存的、用于恢复与特定 Server 之间已配对关系的信息集合。设备会话关联一个 Server 和一个已配对设备。
_Avoid_：登录态、配对请求

**设备密钥（Device Key）**：
Server 授予单个已配对设备的长期访问凭据。设备密钥不同于用于建立关系的短期配对码。
_Avoid_：配对码、全局 Token

**可信局域网（Trusted LAN）**：
由使用者控制并被 EzWinCommand 安全模型信任的本地网络边界。公网、访客网络和其他不受信任网络不属于可信局域网。
_Avoid_：互联网、任意内网

## 媒体状态

**媒体快照（Media Snapshot）**：
某一时刻媒体、音量和音频设备状态的一致视图。媒体快照以修订号标识其新旧关系。
_Avoid_：媒体事件、局部更新

**修订号（Revision）**：
标识媒体快照先后关系的单调序号。修订号用于判断状态是否更新，不代表媒体内容本身的版本。
_Avoid_：时间戳、媒体 ID

## 复合控制

**电竞模式（Esports Mode）**：
协调通信、游戏和音频环境进入或退出目标状态的复合控制动作。它不是单个进程启动命令。
_Avoid_：游戏启动器、电竞进程
