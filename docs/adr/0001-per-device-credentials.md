# 为每个已配对设备签发独立凭据

EzWinCommand 为每个已配对设备签发独立设备密钥，并以设备为单位鉴权和撤销，而不让所有客户端共享全局 Token，也不引入面向公网账号体系。共享 Token 无法隔离单个设备泄露或撤销，完整账号体系则超出可信局域网产品所需的复杂度；每设备凭据使 Server 能独立管理设备关系，并让该设备的活动连接随撤销失效。

## Consequences

Server 必须持久化设备关系并保护设备密钥；客户端必须安全保存自己的设备会话。配对码只用于建立关系，不能代替设备密钥承担长期鉴权。
