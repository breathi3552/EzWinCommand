# 媒体状态使用 Snapshot 与 SSE 同步

EzWinCommand 以完整媒体快照作为一致性基线，并通过带单调修订号的 SSE 发布后续变化；断线客户端按修订号恢复，慢消费者以最新状态优先。固定周期轮询会在状态不变时持续触发 Windows 集成、重复读取封面和设备，并可能让迟到响应覆盖新状态；Snapshot 与 SSE 将刷新成本绑定到真实变化，同时保留明确的恢复路径。

## Consequences

Server 必须保证快照与事件注册之间不丢失变化，并定义有限回放和慢消费者策略。客户端必须拒绝过期修订，在断线或无法回放时重新取得完整快照。
